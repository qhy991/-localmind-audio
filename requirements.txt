# Runtime
numpy>=1.26

# Dev / test
pytest>=8
imageio-ffmpeg>=0.5  # local ffmpeg binary for m4a/mp3 decode tests

# ML backends (installed on demand for STT/LLM milestones, not needed for
# provisioning + audio-decode foundation):
#   mlx-whisper
#   mlx-lm
